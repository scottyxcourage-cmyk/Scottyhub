{
  "name": "scottyhub",
  "version": "1.0.0",
  "scripts": {
    "build": "cd scottyhub && npm install && npm run build",
    "start": "node scottyhub-backend/server.js",
    "install-all": "npm install --prefix scottyhub-backend && cd scottyhub && npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "nodemailer": "^6.9.7",
    "express-rate-limit": "^7.1.5"
  }
}
